<section class="container">
    <h2>Архив задач</h2>
</section>

<partial name="ViewComponents/_AlertSection"/>

<section class="container mt-4">
    <div class="row align-items-end mb-2">
        <div class="col-lg-8">
            <h5>Всего: <b id="listCount">@ViewData["taskCount"]</b> задач в архиве</h5>
        </div>
    </div>

    @Html.AjaxGrid(Url.Action("ArchiveGrid", new {exitMode = true}))
</section>

<div id="modalContainer"></div>